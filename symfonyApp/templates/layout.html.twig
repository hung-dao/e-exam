{% extends 'base.html.twig' %}

{# this is the navigation bar #}
{% block navbar %}
    <div class="nav-side-menu">
        <div class="brand">
            <a class="navbar-brand" href="{{  path('index') }}">
                <img alt="E-Exam" src=" {{ asset('build/images/logo.png') }}" style="object-fit: cover;">
            </a>
        </div>
        <i class="fa fa-bars fa-2x toggle-btn" data-toggle="collapse" data-target="#menu-content"></i>

        <div class="menu-list">
            <ul id="menu-content" class="menu-content collapse out">
                {# for teacher user display below, for student, uncomment the 2nd#}
                <li class="active">
                    <a href="{{ path("dashboard") }}">
                        <i class="fa fa-dashboard fa-lg"></i> Dashboard
                    </a>
                </li>

                <li class="">
                    <a href="{{ path("create-new-exam") }}">
                        <i class="fa fa-book fa-lg"></i> Create new exam
                    </a>
                </li>

                <li class="">
                    <a href="{{ path("create-new-question") }}">
                        <i class="fa fa-question fa-lg"></i> Create new question
                    </a>
                </li>

                {#TODO this is for student model. add user to display
                <li class="">
                    <a href="{{ path("dashboard") }}">
                        <i class="fa fa-dashboard fa-lg"></i> My Exams
                    </a>
                </li>

                <li class="">
                    <a href="{{ path("open-exam") }}">
                        <i class="fa fa-question fa-lg"></i> Exams
                    </a>
                </li>#}

                <li class="bar-footer button">
                    <ul>
                        <li> {#display only when user not logged in#}
                            <a href="{{ path("login") }}" class="btn navbar-btn btn-default">
                                Login
                            </a>
                        </li>
                        {#below is for logged
                        <li class="">
                            <a href="#">
                                <i class="fa fa-user fa-lg"></i> Hello
                            </a>
                        </li>
                        <li>
                            <a href="#" class="btn navbar-btn btn-default">
                                <i class="fa fa-sign-out fa-lg"></i> Log out
                            </a>
                        </li>#}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}

